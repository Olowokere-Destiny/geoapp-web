How to run

1. Clone the repo into a folder or desktop
2. Navigate to the root of the project
3. Install dependencies by running "npm install" in the terminal
4. Start the project by running "npm run dev"
5. Visit http://localhost:5173 on the browser
6. Log in using the details in the backend seed file
